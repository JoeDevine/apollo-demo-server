extend schema
  @link(url: "https://specs.apollo.dev/federation/v2.0", import: ["@key"])

type Query {
  userDetails: User
}

type User @key(fields: "id") {
  id: ID!
  name: String!
  Surname: String!
}
