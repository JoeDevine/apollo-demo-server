extend schema
  @link(
    url: "https://specs.apollo.dev/federation/v2.3"
    import: ["@key", "@interfaceObject", "@shareable", "@tag"]
  )

type Query {
  users: [User]
  userById(id: ID!): User
  testCache: TestCache
}

# Temp type to test caching options with RestDataSource
type TestCache {
  result: String
}

type User @key(fields: "id") {
  id: ID!
  name: String!
  surname: String!
  items: [Item]
}

type Item @key(fields: "id") @interfaceObject {
  id: ID!
  size: Int
  weight: Int
}
